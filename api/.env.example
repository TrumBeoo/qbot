# api/.env.example -> cp .env.example .env
#
# Chay tu HOST (prisma migrate tren may): dung localhost.
# Chay TRONG container: doi localhost thanh postgres (ten service trong compose).
# Gia tri phai khop POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB o compose.
DATABASE_URL="postgresql://qbot:qbot@localhost:5432/qbot?schema=public"
