# legacy/ chi con la lop CRUD + auth tren MySQL/MongoDB.
#
# Toan bo phan AI (RAG, TTS, nhan dien ngon ngu) da chuyen sang ai/ va duoc
# goi qua HTTP, nen bo duoc 10 goi nang: torch, transformers,
# sentence-transformers, faiss-cpu, huggingface-hub, edge-tts, langdetect,
# va ba goi langchain (chi memory_service dung, ma no da bi xoa).
# Anh Docker tu 2.43GB xuong con vai tram MB.

Flask==3.1.1
flask-cors==6.0.1
python-dotenv==1.1.1
requests==2.32.4
pytz==2025.2

# Database legacy
pymongo==4.13.2
mysql-connector-python==9.1.0

# Auth
PyJWT==2.8.0
bcrypt==4.3.0
